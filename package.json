{
  "name": "mern-root",
  "version": "1.0.0",
  "description": "Root package.json for MERN project",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
